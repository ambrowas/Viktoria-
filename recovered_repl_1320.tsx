"                {/* Options (if multiple choice or options present) */}\n                {(active.question.type === \"MULTIPLE_CHOICE\" || (active.question.options && active.question.options.filter(o => o && o.trim() !== \"\").length > 0)) && (\n                  <div>\n                    <h3 className=\"text-xs font-black uppercase tracking-widest text-[#fca311] mb-3\">Options</h3>\n                    <div className=\"grid gap-3\">\n                      {(active.question.options?.filter(o => o && o.trim() !== \"\") || []).map((option, idx) => {\n                        const letter = [\"A\", \"B\", \"C\", \"D\"][idx] || \"\";\n                        const isCorrectOption = option.trim().toLowerCase() === active.question.correctAnswer.trim().toLowerCase();\n                        return (\n                          <div\n                            key={`${option}-${idx}`}\n                            className={`flex items-center gap-3 px-4 py-3 rounded-xl border text-sm font-bold transition-all ${\n                              isCorrectOption\n                                ? \"bg-emerald-500/10 border-emerald-500 text-emerald-300 shadow-[0_0_15px_rgba(16,185,129,0.15)]\"\n                                : \"bg-black/20 border-white/10 text-slate-300\"\n                            }`}\n                          >\n                            <span className={`w-6 h-6 rounded-full flex items-center justify-center text-xs font-black shrink-0 ${\n                              isCorrectOption ? \"bg-emerald-500 text-white\" : \"bg-white/10 text-white\"\n                            }`}>\n                              {letter}\n                            </span>\n                            <span className=\"flex-1\">{option}</span>\n                            {isCorrectOption && (\n                              <span className=\"text-[10px] font-black uppercase tracking-wider bg-emerald-500/20 px-2 py-0.5 rounded text-emerald-400 border border-emerald-500/30\">\n                                Correct Answ
<truncated 237 bytes>